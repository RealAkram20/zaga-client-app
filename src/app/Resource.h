#pragma once

// Icon resource shared by the app and the installer. The first icon in an exe (by
// numeric id) is what Explorer, the taskbar, and shortcuts show, so keep this low.
#define IDI_ZAGA 101
